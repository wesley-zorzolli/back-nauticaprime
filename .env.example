# Copie este arquivo para `.env` e preencha os valores antes de rodar o container
# Exemplos mínimos para rodar localmente com o `docker-compose.yml` fornecido.

# Porta em que a API irá escutar
PORT=3000

# Banco de dados (PRISMA) — conectado ao serviço `db` do docker-compose
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://postgres:postgres@db:5432/nautica
DIRECT_URL=postgresql://postgres:postgres@db:5432/nautica

# Chave usada para gerar JWT (substitua por uma string segura)
JWT_KEY=troque_esta_chave_por_uma_segura

# Mailtrap (opcional) - usado pelo serviço de e-mail
MAILTRAP_HOST=smtp.mailtrap.io
MAILTRAP_PORT=2525
MAILTRAP_USER=seu_user_mailtrap
MAILTRAP_PASS=sua_senha_mailtrap
MAIL_FROM="Nautica Prime <no-reply@nauticaprime.local>"

# NODE_ENV
NODE_ENV=development

# Opções extras (ex.: chaves de terceiros) — deixe vazias se não usar
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_WHATSAPP_FROM=
